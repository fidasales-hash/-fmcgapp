export interface Product {
  id: string;
  name: string;
  size: string;
  bestBefore: string; // YYYY-MM-DD
  category: string;
  notes: string;
  photoUrl: string;
  photoUrl2: string;
  addedAt: string; // ISO
}
